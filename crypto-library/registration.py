def register_user(username: str, password: str, server_url: str) -> bool:
    # TODO: generate KeyPair, derive KEK, encrypt private key, publish public key,
    #       store encrypted private key locally, then POST registration to server.
    return False
